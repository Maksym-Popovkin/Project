name: Call reusable 2

on: 
 workflow_call:
    inputs: 
      name:
        required: true
        type: string

jobs:
 pre-build:
  runs-on: ubuntu-latest
  outputs:
      service-projects: ${{ steps.configuration.outputs.service-projects }}
    
  steps:
    - name: Clone BE repository
      uses: actions/checkout@v3
    
#     - name: Read Configuration
#       id: configuration
#       uses: CumulusDS/get-yaml-paths-action@v1.0.1
#       with:
#         file: sln.build.yml
#         service-projects: '"services"[]'
               
    - name: test
      id: configuration
      run: |
        service-projects=["src/Services.TimeClock","src/Services.TimeClock","src/Service.Chat","src/Service.Chat"]
        echo $service-projects
        echo "service-projects=$service-projects" >> $GITHUB_OUTPUT
      
    - name: test2
      run: echo ${{ steps.configuration.outputs.service-projects }}
 call_reuable2:
   needs: pre-build
   strategy:
      fail-fast: false
      matrix: 
        source: ${{ fromJson(needs.pre-build.outputs.service-projects) }}
   uses: Maksym-Popovkin/Project/.github/workflows/reusable.yaml@master
   with:
     source: ${{ matrix.source }}
