name: Reusable

on: 
 workflow_call:

jobs:
 test:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/download-artifact@v3
      with:
        name: my-artifact2
    
    - name: Display structure of downloaded files
      run: |
        ls -la
        cd test 
        cat test.txt
        echo "-----------"
        cd test2
        cat test.txt
