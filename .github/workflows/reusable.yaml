name: Reusable

on: 
 workflow_call:
    inputs: 
      source:
        required: true
        type: string
        
      dotnet-version:
        required: true
        type: string
        
      name:
        required: true
        type: string

jobs:
 test:
  runs-on: ubuntu-latest
  steps:
    - name: Get changed files
      run: |
        echo "test"
        res=$(dirname ${{ inputs.dotnet-version }})
        echo $res
