name: Reusable

on: 
 workflow_call:
    inputs: 
#       source:
#         required: true
#         type: string
        
      dotnet-version:
        required: true
        type: string
        
      microservice-name:
        required: true
        type: string

jobs:
 test:
  runs-on: ubuntu-latest
  steps:
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ inputs.dotnet-version }}
    - name: Get changed files
      run: |
        echo "test"
        echo ${{ inputs.microservice-name }}
